# simple-bot
A minimal DevOps demonstration for a containerized trading bot (Binance Testnet)

## Configuration

Secrets are never committed to the repository.  
Real API keys are injected at runtime via environment variables (e.g. Headnotes).  
`.env.example` documents required variables using placeholders only.
